[webservers]
%{ for i in webservers ~}
${i.name} ansible_host=${coalesce(i.network_interface.0.nat_ip_address, i.network_interface.0.ip_address)} fqdn=${i.fqdn}
%{ endfor ~}

[databases]
%{ for key, i in databases ~}
${i.name} ansible_host=${coalesce(i.network_interface.0.nat_ip_address, i.network_interface.0.ip_address)} fqdn=${i.fqdn}
%{ endfor ~}

[storage]
%{ for i in storage ~}
${i.name} ansible_host=${coalesce(i.network_interface.0.nat_ip_address, i.network_interface.0.ip_address)} fqdn=${i.fqdn}
%{ endfor ~}